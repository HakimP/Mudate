<!DOCTYPE html>
<html>
<head>
  <title>Mudate</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= stylesheet_link_tag    'general', media: 'all', 'data-turbolinks-track' => true %>
  <%= stylesheet_link_tag    'precioItem', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
<ul class ="nav navbar-nav navbar-right">
  <% if user_signed_in? %>
    <li>
      <a href = "<%= destroy_user_session_path %>" data-method ="delete">
        Salir
      </a>
    </li>
  <% else %>
    <li>
      <a href ="<%= new_user_session_path %>">
        Login
      </a>
    </li>
    <li>
      <a href =" <%= new_user_registration_path %>">
        Registro
      </a>
    </li>
  <% end %>
</ul>

<nav class="navbar navbar-default navbar-inverse" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Mudate</span>
        <span class="icon-bar"></span>
      </button>
      <%= link_to "Mudate", general_index_path, class:'navbar-brand'%></a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li><%= link_to "Servicios", servicios_path%></li>
        <li><a href="#">Mudanzas de Oficina</a></li>
        <li><a href="#">Servicios Opcionales</a></li>
        <li><%= link_to "Contacto", general_contacto_path%></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
        <li>
          <p class="navbar-text">Hola <%= current_user.email %> <%= link_to "Sign out", destroy_user_session_path, :method => 'delete'%></p>
        </li>
        <% else %>
        <li>
          <p class="navbar-text">¿Ya tienes una cuenta?</p>
        </li>
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">
            <b>Login</b>
            <span class="caret"></span>
          </a>
    			<ul id="login-dp" class="dropdown-menu">
    				<li>
    					 <div class="row">
    							<div class="col-md-12">
    								Login via
    								<div class="social-buttons">
    									<a href="#" class="btn btn-fb"><i class="fa fa-facebook"></i> Facebook</a>
    									<a href="#" class="btn btn-tw"><i class="fa fa-twitter"></i> Twitter</a>
    								</div>
                    or
                     <!-- acdc -->
                     <%= render :file => '/devise/sessions/new' %>
                     <!-- acdc -->
    							</div>
    							<div class="bottom text-center">
    								New here ? <a href="#"><b>Join Us</b></a>
    							</div>
    					 </div>
    				</li>
    			</ul>
        </li>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
<%= yield %>

</body>
</html>
